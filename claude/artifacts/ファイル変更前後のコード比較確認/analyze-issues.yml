# .github/workflows/analyze-issues.yml
# ============================================================
# 問題・課題 自動解析ワークフロー
# Claude API 不使用 / ルールベース完全自動
#
# トリガー:
#   - logs/features/ 以下の .jsonl が main に Push されたとき
#   - logs/screenshots/ 以下が main に Push されたとき
#   - Actions タブから手動実行も可能
#
# 処理:
#   1. 全 .jsonl を 11種のルールで解析 → issues.json 生成
#   2. logs/screenshots/ を docs/screenshots/ にコピー（GitHub Pages配信用）
#   3. index.html（可視化ダッシュボード）を生成
#   4. docs/ を自動コミット & Push
# ============================================================

name: 問題・課題 自動解析

on:
  push:
    branches:
      - main                          # main ブランチのみ対象（他ブランチのPushでは起動しない）
    paths:
      - 'logs/features/**.jsonl'      # ログ追加時
      - 'logs/screenshots/**'         # スクショ追加時

  # 手動実行（ログ追加なしでも再解析したいとき）
  workflow_dispatch:
    inputs:
      reason:
        description: '手動実行の理由（任意）'
        required: false
        default: '手動トリガー'

jobs:
  analyze:
    name: ログ解析 → ダッシュボード生成
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      # ── 1. チェックアウト ─────────────────────────────────────────────────
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0              # 全履歴取得（差分検出精度向上）

      # ── 2. Node.js セットアップ ───────────────────────────────────────────
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'               # npm キャッシュ有効（ビルド時間短縮）

      # ── 3. 依存パッケージインストール ─────────────────────────────────────
      - name: Install dependencies
        run: npm ci

      # ── 4. ログ解析（11ルールベース問題抽出・優先度算出）────────────────
      - name: Analyze logs
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "ステップ1: ログ解析・問題抽出"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          node scripts/analyze-logs.js

      # ── 5. スクショを docs/ にコピー（GitHub Pages 配信用）─────────────
      - name: Copy screenshots to docs/
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "ステップ2: スクショをGitHub Pages用にコピー"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          if [ -d "logs/screenshots" ]; then
            mkdir -p docs/screenshots
            cp -r logs/screenshots/. docs/screenshots/
            echo "スクショコピー完了:"
            find docs/screenshots -name "*.jpg" -o -name "*.png" | wc -l
          else
            echo "screenshots ディレクトリなし（スキップ）"
          fi

      # ── 6. ダッシュボードHTML生成 ─────────────────────────────────────────
      - name: Generate dashboard
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "ステップ3: ダッシュボードHTML生成"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          node scripts/generate-dashboard.js

      # ── 7. 解析結果サマリをActionsログに出力 ─────────────────────────────
      - name: Print summary
        run: |
          node -e "
            const d = JSON.parse(require('fs').readFileSync('docs/issues/issues.json','utf8'));
            const s = d.summary;
            console.log('');
            console.log('📊 解析結果サマリ');
            console.log('─────────────────────────────────');
            console.log('総問題数 :', s.totalIssues);
            console.log('Critical :', s.critical);
            console.log('High     :', s.high);
            console.log('Medium   :', s.medium);
            console.log('Low      :', s.low);
            console.log('解析ログ :', s.totalLogs, '件');
            console.log('対象画面 :', s.totalFeatures, '画面');
            console.log('─────────────────────────────────');
            if (s.critical > 0) {
              console.log('⚠️  Critical 問題があります。即時対応を確認してください。');
              process.exitCode = 0;
            }
          "

      # ── 8. 自動コミット & Push ────────────────────────────────────────────
      - name: Commit and push dashboard
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add docs/

          git diff --cached --quiet && {
            echo "変更なし。コミットをスキップします。"
            exit 0
          }

          TRIGGER="${{ github.event_name }}"
          # [skip ci] を付与してこのコミット自体がワークフローを再起動しないようにする
          MSG="docs: 問題課題ダッシュボード自動更新 [${TRIGGER}] [skip ci]"
          git commit -m "$MSG"
          git push origin main
