# .github/workflows/analyze-issues.yml
# ============================================================
# 問題・課題 自動解析ワークフロー
# Claude API 不使用 / ルールベース完全自動
#
# トリガー:
#   - logs/features/ 以下の .jsonl が main に Push されたとき
#   - Actions タブから手動実行も可能
#
# 処理:
#   1. 全 .jsonl を 10種のルールで解析
#   2. issues.json（構造化問題データ）を生成
#   3. index.html（可視化ダッシュボード）を生成
#   4. docs/issues/ を自動コミット & Push
# ============================================================

name: 問題・課題 自動解析

on:
  push:
    branches:
      - main
    paths:
      - 'logs/features/**.jsonl'

  # 手動実行（ログ追加なしでも再解析したいとき）
  workflow_dispatch:
    inputs:
      reason:
        description: '手動実行の理由（任意）'
        required: false
        default: '手動トリガー'

jobs:
  analyze:
    name: ログ解析 → ダッシュボード生成
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      # ── 1. チェックアウト ─────────────────────────────────────────────────
      - name: Checkout
        uses: actions/checkout@v4

      # ── 2. Node.js セットアップ ───────────────────────────────────────────
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      # ── 3. 依存パッケージインストール（express/cors のみ・API SDK不要）───
      - name: Install dependencies
        run: npm ci

      # ── 4. ログ解析（ルールベース問題抽出・優先度算出）────────────────────
      - name: Analyze logs
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "ステップ1: ログ解析・問題抽出"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          node scripts/analyze-logs.js

      # ── 5. ダッシュボードHTML生成 ─────────────────────────────────────────
      - name: Generate dashboard
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "ステップ2: ダッシュボードHTML生成"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          node scripts/generate-dashboard.js

      # ── 6. サマリをActionsログに出力 ─────────────────────────────────────
      - name: Print summary
        run: |
          node -e "
            const d = JSON.parse(require('fs').readFileSync('docs/issues/issues.json','utf8'));
            const s = d.summary;
            console.log('');
            console.log('📊 解析結果サマリ');
            console.log('─────────────────────────────────');
            console.log('総問題数 :', s.totalIssues);
            console.log('Critical :', s.critical);
            console.log('High     :', s.high);
            console.log('Medium   :', s.medium);
            console.log('Low      :', s.low);
            console.log('解析ログ :', s.totalLogs, '件');
            console.log('─────────────────────────────────');
            if (s.critical > 0) console.log('⚠️  Critical 問題があります。即時対応を確認してください。');
          "

      # ── 7. 自動コミット & Push ────────────────────────────────────────────
      - name: Commit and push dashboard
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add docs/issues/

          git diff --cached --quiet && {
            echo "変更なし。コミットをスキップします。"
            exit 0
          }

          TRIGGER="${{ github.event_name }}"
          MSG="docs: 問題課題ダッシュボード自動更新 [${TRIGGER}] [skip ci]"
          git commit -m "$MSG"
          git push
