# .github/workflows/generate-review.yml
# ============================================================
# アクションレビューHTML 自動生成ワークフロー
#
# [v3.1 修正]
#   ① workflow_run の workflows 名を実際の名前に修正
#      変更前: ['問題・課題 自動解析']  ← 不一致で連鎖起動しなかった
#      変更後: ['Analyze Issues & Publish Dashboard']
#   ② push トリガーを追加
#      logs/features/*.jsonl または logs/screenshots/** に Push された
#      時点で直接起動（analyze-issues を経由しない場合にも対応）
#
# トリガー（3系統）:
#   1. push: logs/features/*.jsonl または logs/screenshots/** の変更
#   2. workflow_run: analyze-issues.yml 完了後に自動連鎖
#   3. workflow_dispatch: Actions タブから手動実行
#
# 処理:
#   1. logs/features/*.jsonl → シーケンス構築
#   2. logs/screenshots/     → スクショ収集（docs/screenshots/ にコピー）
#   3. docs/issues/issues.json → 課題データ照合
#   4. docs/review/index.html → アクションレビューHTML生成
#   5. docs/ を自動コミット & Push → GitHub Pages に反映
#
# GitHub Pages URL:
#   https://karkyon.github.io/log-server/review/
# ============================================================

name: アクションレビューHTML生成

on:
  # ── 1. logs/features/ または logs/screenshots/ への Push で直接起動 ──
  push:
    branches:
      - main
    paths:
      - 'logs/features/*.jsonl'
      - 'logs/screenshots/**'

  # ── 2. 手動実行 ──────────────────────────────────────────────────────
  workflow_dispatch:
    inputs:
      reason:
        description: '実行理由（任意）'
        required: false
        default: '手動生成'

  # ── 3. analyze-issues.yml 完了後に自動連鎖 ────────────────────────────
  #       ★修正: workflow_run の workflows 名を analyze-issues.yml の
  #              実際の name フィールド値に合わせる
  workflow_run:
    workflows: ['Analyze Issues & Publish Dashboard']
    types: [completed]
    branches: [main]

jobs:
  generate-review:
    name: アクションレビューHTML生成
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      # ── 1. チェックアウト ─────────────────────────────────────────────
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # ── 2. Node.js セットアップ ───────────────────────────────────────
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      # ── 3. 依存パッケージインストール ─────────────────────────────────
      - name: Install dependencies
        run: npm ci

      # ── 4. スクショを docs/ にコピー（GitHub Pages で配信するため）──
      - name: Copy screenshots to docs/
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "スクショを docs/screenshots/ にコピー"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          if [ -d "logs/screenshots" ]; then
            mkdir -p docs/screenshots
            cp -r logs/screenshots/. docs/screenshots/
            echo "コピー完了 — 画像ファイル数:"
            find docs/screenshots -name "*.jpg" -o -name "*.png" | wc -l
          else
            echo "screenshots ディレクトリなし（スキップ）"
          fi

      # ── 5. アクションレビューHTML生成 ────────────────────────────────
      - name: Generate review HTML
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "アクションレビューHTML生成 (generate-review.js v3.1)"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          node scripts/generate-review.js

      # ── 6. 生成結果サマリ出力 ─────────────────────────────────────────
      - name: Print summary
        run: |
          node -e "
            const fs = require('fs');
            const html = fs.readFileSync('docs/review/index.html','utf8');
            const size = (Buffer.byteLength(html,'utf8')/1024).toFixed(1);
            const pageMatches = html.match(/class=\"page\"/g) || [];
            const seqMatches  = html.match(/class=\"action-log\"/g) || [];
            console.log('');
            console.log('📋 レビュー生成サマリ');
            console.log('─────────────────────────────────');
            console.log('ファイルサイズ :', size + ' KB');
            console.log('画面ページ数  :', pageMatches.length - 1, '（課題一覧除く）');
            console.log('シーケンス数  :', seqMatches.length);
            console.log('─────────────────────────────────');
            console.log('📎 URL: https://karkyon.github.io/log-server/review/');
          "

      # ── 7. 自動コミット & Push ────────────────────────────────────────
      - name: Commit and push
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add docs/

          git diff --cached --quiet && {
            echo "変更なし。コミットをスキップします。"
            exit 0
          }

          TRIGGER="${{ github.event_name }}"
          # [skip ci] を付与してこのコミット自体がワークフローを再起動しないようにする
          MSG="docs: アクションレビュー自動更新 [${TRIGGER}] [skip ci]"
          git commit -m "$MSG"
          git push origin main
